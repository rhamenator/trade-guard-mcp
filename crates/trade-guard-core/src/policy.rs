//! Deterministic pre-trade policy: buying power, margin, position/loss/
//! drawdown limits, price bands, session/calendar checks,
//! `check-evidence-eligibility` (verifies a signed `EvidenceBundle` from
//! `market-intelligence-mcp` before any live intent is authorized).
//! Status: not-started.
